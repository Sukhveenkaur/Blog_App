{{!-- 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs</title>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/">home</a></li>
            <li><a href="/myblog">myblog</a></li>
            <li><a href="/blogs">all blogs</a></li>
        </ul>
    </nav>
    
    <h1>All Blogs</h1>

    {{#each blogs as |blog|}}
        <div>
            <p>{{blog.user.username}}</p>
            <h1>{{blog.title}}</h1>
            <p>{{blog.content}}</p>
            {{#if blog.imageUrl}}
                <img src="{{blog.imageUrl}}" alt="Blog Image">
            {{/if}}
        </div>
    {{/each}}
</body>
</html>

  --}}
  {{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs</title>
    <!-- Include Tailwind CSS stylesheet -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-100 font-sans">

    <nav class="bg-blue-500 p-4">
        <ul class="flex">
            <li class="mr-4"><a href="/" class="text-white">home</a></li>
            <li class="mr-4"><a href="/myblog" class="text-white">myblog</a></li>
            <li class="mr-4"><a href="/blogs" class="text-white">all blogs</a></li>
        </ul>
    </nav>
    
    <h1 class="text-4xl font-bold mt-8 mb-4">All Blogs</h1>

    {{#each blogs as |blog|}}
        <div class="max-w-md mx-auto bg-white p-6 mb-4 rounded-md shadow-md">
            <p class="text-gray-500">{{blog.user.username}}</p>
            <h1 class="text-2xl font-bold mb-2">{{blog.title}}</h1>
            <p class="text-gray-800">{{blog.content}}</p>
            {{#if blog.imageUrl}}
                <img src="{{blog.imageUrl}}" alt="Blog Image" class="mt-4">
            {{/if}}
        </div>
    {{/each}}

</body>
</html> --}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs</title>
    <!-- Include Tailwind CSS stylesheet -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-100 font-sans">
      {{#if user}}
    <nav class="bg-blue-500 p-4">
        <ul class="flex">

            <li class="mr-4"><a href="/" class="text-white">Home</a></li>
            <li class="mr-4"><a href="/myblog" class="text-white">Myblogs</a></li>
            <li class="mr-4"><a href="/blogs" class="text-white">All blogs</a></li>
        </ul>
    </nav>
    {{/if}}
    
    <h1 class="text-4xl font-bold mt-8 mb-4">All Blogs</h1>

    <div class="ml-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-4">
        {{#each blogs as |blog|}}
            <div class="max-w-md bg-white p-6 rounded-md shadow-md">
                <p class="text-gray-500">{{blog.user.username}}</p>
                <h1 class="text-2xl font-bold mb-2">{{blog.title}}</h1>
                <p class="text-gray-800">{{blog.content}}</p>
                {{#if blog.imageUrl}}
                    <img src="{{blog.imageUrl}}" alt="Blog Image" class="mt-4">
                {{/if}}
            </div>
        {{/each}}
    </div>

</body>
</html>
